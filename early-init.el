;;; -*- mode: emacs-lisp; coding: utf-8; -*-

;;; Commentary:

;; Emacs 27+ loads this file before (normally) calling `package-initialize'.

;;; Code:

;; (setq package-enable-at-startup nil)

(provide 'early-init)

;;; early-init.el ends here
